# Dummy values so config.js's required-var check passes during `npm test`.
# Not real credentials — do not put secrets here.
TELEGRAM_BOT_TOKEN=test-token
TELEGRAM_CHAT_ID=test-chat
FLARE_SOLVER_URL=http://localhost:8191
DB_PATH=:memory:
